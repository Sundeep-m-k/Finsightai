Use the following context to explain a precomputed financial plan for this student.

## Macro context (current)
{MACRO_CONTEXT}

## Student profile (summary)
{PROFILE_SUMMARY}

## Deterministic plan outcomes (SOURCE OF TRUTH)
{PLAN_OUTCOMES}

## Retrieved source material (supporting rationale only)
{RAG_CHUNKS}

## Your task
Produce a JSON object only (no markdown, no code fence). Use this exact structure:

{{
  "insights": [
    {{
      "recommendation": "one clear recommendation",
      "principle": "the financial principle behind it",
      "behavioral_flags": ["list of flags from the profile that triggered this"],
      "sources": [
        {{
          "title": "exact source_title from the chunk you used",
          "url": "exact source_url from that chunk",
          "preview": "1-2 sentence preview from the chunk (max 200 chars)",
          "relevance_score": 0.0 to 1.0 (use the score from the chunk),
          "badge_type": "government" | "academic" | "finsight-kb"
        }}
      ]
    }}
  ],
  "action_plan": [
    {{
      "title": "short step title",
      "description": "what to do",
      "time_label": "Month 1" or "Month 2" or "Month 3"
    }}
  ],
  "narrative": "One short paragraph in a warm, mentor-like tone summarizing the situation and encouragement.",
  "disclaimer": "FinSight AI provides educational insights and planning support — not personalized financial advice. For major financial decisions, consult a certified financial planner."
}}

Hard rules:
1) Use plan outcomes as the single source of truth for all numbers, amounts, timelines, readiness status, and priorities.
2) Do NOT invent or change scores, amounts, ETFs, allocations, blockers, or milestones.
3) RAG chunks are only for educational rationale and citations.
4) If a value is missing in plan outcomes, avoid fabricating; explain qualitatively.
5) Emergency fund targets are deterministic: use emergency_fund_1mo_target, emergency_fund_3mo_target, and starter_emergency_target EXACTLY. Do not calculate or invent different amounts.

Content rules:
- Give 3-4 insights.
- Each insight must reference at least one source from retrieved chunks when available.
- Keep action plan aligned with plan outcomes and exactly 3 steps (90-day plan).

Output only the JSON object.
