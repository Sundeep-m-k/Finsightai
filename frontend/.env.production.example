# Copy to .env.production or set these in Vercel → Project → Settings → Environment Variables
# Use Production when deploying. Required for live API calls.

VITE_BACKEND_URL=https://finscope-backend.onrender.com
VITE_AI_SERVICE_URL=https://finscope-ai-service.onrender.com
# Leave unset or false so the app uses real backend + AI service
# VITE_USE_MOCK=false
